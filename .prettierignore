# The shipped design system is a handoff artifact, not our source. Reformatting
# it would make every future comparison against it noisy.
mockups-and-design-system/

# Vendored agent skills. Same reasoning: they are upstream files that happen to
# live here, and reformatting them buries the next update's real diff.
#
# `.claude/` is ignored wholesale rather than down to `.claude/skills/`, which
# also takes `.claude/settings.json` — our own file — out of the check. That is
# deliberate: `.claude/worktrees/` holds full checkouts of this repository, and
# a prettier run that descended into it would format the same tree several
# times over.
.agents/
.claude/
skills-lock.json

# Generated from the backend's OpenAPI document.
packages/api-client/openapi.json
packages/api-client/src/schema.d.ts

# Generated by scripts/vendor-fonts.mjs from the stylesheet Google serves.
# Reformatting it would put prettier and the generator in a loop, where each run
# of the script leaves the tree dirty.
packages/design-tokens/src/fonts.css

node_modules/
apps/web/dist/
apps/api/target/
e2e/test-results/
e2e/playwright-report/
visual/test-results/
visual/playwright-report/
pnpm-lock.yaml
