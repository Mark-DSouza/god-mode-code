**Wordmark** — the brand lockup: the `▚` block mark + `GOD_MODE_CODE` in Share Tech Mono with green glow. Use in headers and the access screen. There is no real logo; this stands in.

```jsx
<Wordmark size={18} />
<Wordmark size={26} glow="lg" />
<Wordmark mark={false} />
```
