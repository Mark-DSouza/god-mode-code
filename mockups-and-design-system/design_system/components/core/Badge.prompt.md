**Badge** — a small pill for status and labels.

```jsx
<Badge tone="green" dot>Online</Badge>
<Badge tone="warning">New Best</Badge>
<Badge tone="error" solid>Failed</Badge>
```
Tones `green|neutral|error|warning|info`; `solid` fills, `dot` adds a status dot.
