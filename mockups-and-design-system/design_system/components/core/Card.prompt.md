**Card** — the default panel/surface; hard-cornered, hairline border, faint outer glow.

```jsx
<Card glow scanlines padding="var(--space-6)">…</Card>
<Card interactive onClick={pick}>…</Card>
```
`glow` = green edge bloom, `scanlines` = CRT overlay, `interactive` = hover lift.
