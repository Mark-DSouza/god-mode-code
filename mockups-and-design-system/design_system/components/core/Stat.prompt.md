**Stat** — a big VT323 CRT numeral with a small terminal caption; for WPM, accuracy, streaks.

```jsx
<Stat value={112} unit="wpm" label="Speed" />
<Stat value="98.4" unit="%" label="Accuracy" accent="warning" size="md" />
```
`accent` recolors the value + glow; sizes `sm|md|lg`.
