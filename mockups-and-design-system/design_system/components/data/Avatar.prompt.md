**Avatar** — a square terminal identity tile (initials, or an image), with green glow.

```jsx
<Avatar initials="YU" />
<Avatar src="/me.png" size="lg" />
```
`size` is a px number or `sm|md|lg`; `glow` toggles the phosphor ring.
