**EmptyState** — a centered no-data placeholder (dim glyph + title + copy + action).

```jsx
<EmptyState title="No runs logged yet" description="Complete a run and the rain starts keeping score.">
  <Button size="lg">Start Your First Run</Button>
</EmptyState>
```
