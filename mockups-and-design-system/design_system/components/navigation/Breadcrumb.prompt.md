**Breadcrumb** — a monospace path trail; the last segment glows green as the active leaf.

```jsx
<Breadcrumb items={[
  { label: "godmodecode", onClick: goHome },
  { label: "code", onClick: goCat },
  { label: "two-sum" }]} />
```
