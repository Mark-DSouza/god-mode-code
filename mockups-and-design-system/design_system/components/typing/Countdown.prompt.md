**Countdown** — the "GET READY" pre-run screen; a giant white-cored shine numeral over a dimmed passage preview.

```jsx
<Countdown count={3} tag="Code · Principal"
  preview="export function clamp(x, lo, hi) { return Math.min(hi, Math.max(lo, x)); }" />
```
Presentational — drive `count` from a 3→1 timer.
