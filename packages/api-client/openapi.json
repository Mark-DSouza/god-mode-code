{
  "openapi" : "3.1.0",
  "info" : {
    "title" : "GOD_MODE_CODE API",
    "description" : "Backend for GOD_MODE_CODE.",
    "version" : "0.0.1"
  },
  "servers" : [ {
    "url" : "/",
    "description" : "Same origin as the application"
  } ],
  "tags" : [ {
    "name" : "Users",
    "description" : "Anyone who has played, Claimed or otherwise"
  }, {
    "name" : "Challenges",
    "description" : "Being handed something to type"
  }, {
    "name" : "Leaderboards",
    "description" : "Rankings by best Run, per Challenge"
  }, {
    "name" : "Solve Runs",
    "description" : "Runs against a Pattern, judged by execution"
  }, {
    "name" : "Health",
    "description" : "Liveness and dependency status"
  }, {
    "name" : "Patterns",
    "description" : "The Code Discipline's catalogue"
  }, {
    "name" : "Profile",
    "description" : "Personal Bests and recent Runs, derived from a User's Runs"
  }, {
    "name" : "Typing Runs",
    "description" : "Runs against a Passage, verified server-side"
  } ],
  "paths" : {
    "/api/users" : {
      "post" : {
        "tags" : [ "Users" ],
        "summary" : "Create an Unclaimed User",
        "description" : "Creates a User with no credentials attached and a generated Handle, and sets the cookie that recognises this browser as them on later visits. Nothing is asked of the visitor.\n\nA browser that already holds a valid cookie is told who it is instead, with 200 — so a retry, a double submit or a second tab cannot strand the Runs the first User already has.\n",
        "operationId" : "create",
        "parameters" : [ {
          "name" : "gmc_recognition",
          "in" : "cookie",
          "required" : false,
          "schema" : {
            "type" : "string"
          }
        } ],
        "requestBody" : {
          "content" : {
            "application/json" : {
              "schema" : {
                "$ref" : "#/components/schemas/UserCreateRequest"
              }
            }
          }
        },
        "responses" : {
          "201" : {
            "description" : "The Unclaimed User that was created",
            "content" : {
              "application/json" : {
                "schema" : {
                  "$ref" : "#/components/schemas/User"
                }
              }
            }
          },
          "200" : {
            "description" : "This browser was already someone, and still is",
            "content" : {
              "application/json" : {
                "schema" : {
                  "$ref" : "#/components/schemas/User"
                }
              }
            }
          },
          "429" : {
            "description" : "Too many Unclaimed Users have been created from this address recently"
          },
          "403" : {
            "description" : "The challenge widget did not vouch for this browser"
          }
        }
      }
    },
    "/api/users/claim" : {
      "post" : {
        "tags" : [ "Users" ],
        "summary" : "Claim the User this browser is",
        "description" : "Attaches the identity provider's credentials to the User this browser is recognised as, choosing the Handle given in the request body (ADR-0011).\n\nIf those credentials already belong to a different User, this browser's Runs are merged into that User instead — silently and always, because Runs are append-only facts with no conflict to resolve (ADR-0007) — and the response carries a fresh cookie recognising this browser as the account it was merged into.\n",
        "operationId" : "claim",
        "parameters" : [ {
          "name" : "gmc_recognition",
          "in" : "cookie",
          "required" : false,
          "schema" : {
            "type" : "string"
          }
        } ],
        "requestBody" : {
          "content" : {
            "application/json" : {
              "schema" : {
                "$ref" : "#/components/schemas/ClaimRequest"
              }
            }
          },
          "required" : true
        },
        "responses" : {
          "200" : {
            "description" : "Claimed, or merged into an existing account",
            "content" : {
              "application/json" : {
                "schema" : {
                  "$ref" : "#/components/schemas/User"
                }
              }
            }
          },
          "404" : {
            "description" : "This browser is nobody yet — there is no User here to Claim"
          },
          "409" : {
            "description" : "The chosen Handle is already taken"
          },
          "401" : {
            "description" : "No valid bearer token was presented"
          }
        },
        "security" : [ {
          "bearerAuth" : [ ]
        } ]
      }
    },
    "/api/typing-runs" : {
      "post" : {
        "tags" : [ "Typing Runs" ],
        "summary" : "Submit a finished Run for Verification",
        "description" : "Takes raw data — the text as typed, the keystroke count, the two timestamps — and recomputes WPM and Accuracy from it. The request body has nowhere to put a client-computed metric, because none would be believed (ADR-0003).\n\nA refusal is 422 with a machine-readable reason, not a 400: the request was well formed and was understood, and it is the Run that did not survive.\n",
        "operationId" : "submit",
        "parameters" : [ {
          "name" : "gmc_recognition",
          "in" : "cookie",
          "required" : false,
          "schema" : {
            "type" : "string"
          }
        } ],
        "requestBody" : {
          "content" : {
            "application/json" : {
              "schema" : {
                "$ref" : "#/components/schemas/TypingRunSubmission"
              }
            }
          },
          "required" : true
        },
        "responses" : {
          "201" : {
            "description" : "The Run as the server computed it",
            "content" : {
              "application/json" : {
                "schema" : {
                  "$ref" : "#/components/schemas/TypingRun"
                }
              }
            }
          },
          "401" : {
            "description" : "This browser is nobody yet"
          },
          "422" : {
            "description" : "The Run was not recorded, and why",
            "content" : {
              "application/json" : {
                "schema" : {
                  "$ref" : "#/components/schemas/Rejection"
                }
              }
            }
          },
          "429" : {
            "description" : "Too many Runs have been submitted by this User or from this address recently"
          }
        }
      }
    },
    "/api/solve-runs" : {
      "post" : {
        "tags" : [ "Solve Runs" ],
        "summary" : "Submit a finished Solve Run to be judged",
        "description" : "Takes the lines that were written, assembles them with the Pattern's Scaffold, and has the judge execute the result against every one of the Pattern's tests. The Verdict comes back from that execution; the duration and the WPM are recomputed from the raw submission (ADR-0003).\n\nA Failed Verdict is a 201. The Solve Run happened, it is recorded, and it belongs to the player — a Solve Run can fail, which is the difference between it and a Typing Run (ADR-0006).\n\n422 is a submission that was not recorded at all, with the reason. 503 is the judge being unreachable: no Verdict exists, nothing about the submitted source can be concluded, and the Issue is left unspent so the same lines can be sent again.\n",
        "operationId" : "submit_1",
        "parameters" : [ {
          "name" : "gmc_recognition",
          "in" : "cookie",
          "required" : false,
          "schema" : {
            "type" : "string"
          }
        } ],
        "requestBody" : {
          "content" : {
            "application/json" : {
              "schema" : {
                "$ref" : "#/components/schemas/SolveRunSubmission"
              }
            }
          },
          "required" : true
        },
        "responses" : {
          "201" : {
            "description" : "The Solve Run as judged and recorded, whatever the Verdict",
            "content" : {
              "application/json" : {
                "schema" : {
                  "$ref" : "#/components/schemas/SolveRun"
                }
              }
            }
          },
          "401" : {
            "description" : "This browser is nobody yet"
          },
          "422" : {
            "description" : "The Solve Run was not recorded, and why",
            "content" : {
              "application/json" : {
                "schema" : {
                  "$ref" : "#/components/schemas/Rejection"
                }
              }
            }
          },
          "503" : {
            "description" : "There is no Verdict to be had. Only the Code Discipline is affected",
            "content" : {
              "application/json" : {
                "schema" : {
                  "$ref" : "#/components/schemas/Unjudged"
                }
              }
            }
          }
        }
      }
    },
    "/api/patterns/{slug}/challenges" : {
      "post" : {
        "tags" : [ "Patterns" ],
        "summary" : "Be handed this Pattern to solve",
        "description" : "Records an Issue against the User — who got what, when, and until when — and returns the Pattern with its Scaffold and Example Tests.\n\nUnlike a Passage, the player names which one. There is nothing to shop for: a Pattern is a technique to practise, not a score to farm.\n\nAsking again abandons whatever Challenge the User was holding, in either Discipline, so nobody can hold several at once and submit whichever went best (ADR-0003). The window is flat rather than scaled by length: a Pattern is answered by thinking, and there is nothing about it to time by the character.\n",
        "operationId" : "request",
        "parameters" : [ {
          "name" : "gmc_recognition",
          "in" : "cookie",
          "required" : false,
          "schema" : {
            "type" : "string"
          }
        }, {
          "name" : "slug",
          "in" : "path",
          "required" : true,
          "schema" : {
            "type" : "string"
          }
        } ],
        "responses" : {
          "201" : {
            "description" : "The Challenge, and when it stops being answerable",
            "content" : {
              "application/json" : {
                "schema" : {
                  "$ref" : "#/components/schemas/SolveChallenge"
                }
              }
            }
          },
          "401" : {
            "description" : "This browser is nobody yet — create a User first"
          },
          "404" : {
            "description" : "No activated Pattern has that slug"
          }
        }
      }
    },
    "/api/patterns/activations" : {
      "post" : {
        "tags" : [ "Patterns" ],
        "summary" : "Run the activation gate over every Pattern awaiting it",
        "description" : "Assembles each inactive Pattern's reference solution with its Scaffold exactly as a player's submission would be, has the judge execute it, and activates the Pattern only if the Verdict is Passed and the judge ran as many tests as the Pattern has.\n\nThat second condition is not a formality. The judge compiles its catalogue into its binary and is deployed separately from the database, so the two can skew — and a judge running an older copy of a Pattern would answer Passed against fewer tests than the curator wrote, which is true and useless.\n\nAnswers 200 whatever happened. A Pattern that did not pass is not an error in this request; it is the gate doing its job, and the body says so per Pattern. Already-activated Patterns are not re-judged and are absent from the answer.\n",
        "operationId" : "activate",
        "responses" : {
          "200" : {
            "description" : "What the gate made of each Pattern that was awaiting it",
            "content" : {
              "application/json" : {
                "schema" : {
                  "type" : "array",
                  "items" : {
                    "$ref" : "#/components/schemas/PatternActivation"
                  }
                }
              }
            }
          }
        }
      }
    },
    "/api/challenges" : {
      "post" : {
        "tags" : [ "Challenges" ],
        "summary" : "Be handed a Passage to transcribe",
        "description" : "Records an Issue against the User — who got what, when, and until when — and returns the Passage. Which Passage arrives is the server's choice.\n\nAsking again abandons the Challenge the User was holding, so a player can skip a Passage they do not fancy, and nobody can hold several at once and submit whichever went best (ADR-0003).\n",
        "operationId" : "request_1",
        "parameters" : [ {
          "name" : "gmc_recognition",
          "in" : "cookie",
          "required" : false,
          "schema" : {
            "type" : "string"
          }
        } ],
        "requestBody" : {
          "content" : {
            "application/json" : {
              "schema" : {
                "$ref" : "#/components/schemas/ChallengeRequest"
              }
            }
          },
          "required" : true
        },
        "responses" : {
          "201" : {
            "description" : "The Challenge, and when it stops being answerable",
            "content" : {
              "application/json" : {
                "schema" : {
                  "$ref" : "#/components/schemas/Challenge"
                }
              }
            }
          },
          "401" : {
            "description" : "This browser is nobody yet — create a User first"
          },
          "404" : {
            "description" : "That Discipline has no Passages. Code never will (ADR-0004)"
          },
          "429" : {
            "description" : "Too many Challenges have been asked for by this User or from this address recently"
          }
        }
      },
      "delete" : {
        "tags" : [ "Challenges" ],
        "summary" : "Give up the Challenge you are holding",
        "description" : "Voids whatever Challenge this User is holding, without recording a Run against it. Leaving mid-Run (the Escape key) calls this so the Issue stops being live immediately, rather than sitting live until the next Challenge is requested.\n\nAnswers 204 whether or not anything was actually live to give up, so a player who has already finished, or never started, is not told anything went wrong.\n",
        "operationId" : "abandon",
        "parameters" : [ {
          "name" : "gmc_recognition",
          "in" : "cookie",
          "required" : false,
          "schema" : {
            "type" : "string"
          }
        } ],
        "responses" : {
          "204" : {
            "description" : "Nothing is live for this User anymore"
          },
          "401" : {
            "description" : "This browser is nobody yet"
          }
        }
      }
    },
    "/api/users/me" : {
      "get" : {
        "tags" : [ "Users" ],
        "summary" : "Read the User this browser is",
        "description" : "Returns the User the request's cookie identifies. 404 means this browser has never been here, or was here longer ago than the cookie lasts — the caller's cue to create one.\n",
        "operationId" : "me",
        "parameters" : [ {
          "name" : "gmc_recognition",
          "in" : "cookie",
          "required" : false,
          "schema" : {
            "type" : "string"
          }
        } ],
        "responses" : {
          "200" : {
            "description" : "The User this browser is recognised as",
            "content" : {
              "application/json" : {
                "schema" : {
                  "$ref" : "#/components/schemas/User"
                }
              }
            }
          },
          "404" : {
            "description" : "This browser is nobody yet"
          }
        }
      }
    },
    "/api/profile" : {
      "get" : {
        "tags" : [ "Profile" ],
        "summary" : "Read this browser's own profile",
        "description" : "Personal Bests per Discipline, best Accuracy, recent average, and the most recent Runs of both kinds interleaved. Every figure is computed from the User's Runs at the moment of the request; none of it is stored.\n\nA User with no Runs is a 200 with empty lists, not a 404. They exist and have simply not played yet, and the screen has an inviting first-Run state to show for it.\n",
        "operationId" : "mine",
        "parameters" : [ {
          "name" : "gmc_recognition",
          "in" : "cookie",
          "required" : false,
          "schema" : {
            "type" : "string"
          }
        } ],
        "responses" : {
          "200" : {
            "description" : "The profile of the User this browser is",
            "content" : {
              "application/json" : {
                "schema" : {
                  "$ref" : "#/components/schemas/Profile"
                }
              }
            }
          },
          "404" : {
            "description" : "This browser is nobody yet"
          }
        }
      }
    },
    "/api/patterns" : {
      "get" : {
        "tags" : [ "Patterns" ],
        "summary" : "Browse the Patterns that can be played",
        "description" : "Every activated Pattern — by Family, then by Seniority, then by name — optionally narrowed to one Family and one Seniority. Each carries its prompt and its Example Tests, so a player can read the contract they will be judged against before starting.\n\nInactive Patterns are absent rather than listed as unavailable. A Pattern is inactive because nobody has yet proved its tests are correct, and offering one would be offering a Challenge that might be unwinnable.\n\nHidden Tests are not here, and there is no endpoint that serves them. They live in the judge's own binary (ADR-0005), and their failure is only ever reported to a player as a count.\n",
        "operationId" : "browse",
        "parameters" : [ {
          "name" : "family",
          "in" : "query",
          "description" : "Only this Family",
          "required" : false,
          "schema" : {
            "type" : "string",
            "description" : "The grouping a Pattern belongs to",
            "enum" : [ "HASH_MAP", "TWO_POINTERS", "SLIDING_WINDOW", "STACK", "HEAP", "BINARY_SEARCH", "GRAPH", "DYNAMIC_PROGRAMMING" ]
          }
        }, {
          "name" : "seniority",
          "in" : "query",
          "description" : "Only this Seniority",
          "required" : false,
          "schema" : {
            "type" : "string",
            "description" : "The difficulty band of a Challenge",
            "enum" : [ "JUNIOR", "SENIOR", "PRINCIPAL" ]
          }
        } ],
        "responses" : {
          "200" : {
            "description" : "The Patterns that match",
            "content" : {
              "application/json" : {
                "schema" : {
                  "type" : "array",
                  "items" : {
                    "$ref" : "#/components/schemas/Pattern"
                  }
                }
              }
            }
          }
        }
      }
    },
    "/api/passages/{passageId}/leaderboard" : {
      "get" : {
        "tags" : [ "Leaderboards" ],
        "summary" : "Read one Passage's Leaderboard",
        "description" : "Users ranked by their best Run on this Passage — only their best, so one fast typist replaying it all afternoon holds one row rather than the top ten. Tied WPMs share a position.\n\nUnclaimed Users are ranked identically to Claimed ones and appear in the same list (ADR-0007).\n\n`entries` is empty until enough distinct Users have attempted the Passage; `participants` and `minimumParticipants` say how far off that is, so the caller can fall back to the Discipline rather than show a ranking of two. `you` carries the requesting User's own row whether or not it is in the published top, and survives the threshold — it is a fact about their own Run rather than a claim about a population.\n",
        "operationId" : "forPassage",
        "parameters" : [ {
          "name" : "passageId",
          "in" : "path",
          "required" : true,
          "schema" : {
            "type" : "string",
            "format" : "uuid"
          }
        }, {
          "name" : "gmc_recognition",
          "in" : "cookie",
          "required" : false,
          "schema" : {
            "type" : "string"
          }
        } ],
        "responses" : {
          "200" : {
            "description" : "The ranking, and where the asker stands in it",
            "content" : {
              "application/json" : {
                "schema" : {
                  "$ref" : "#/components/schemas/Leaderboard"
                }
              }
            }
          },
          "404" : {
            "description" : "No such Passage"
          }
        }
      }
    },
    "/api/health" : {
      "get" : {
        "tags" : [ "Health" ],
        "summary" : "Report the backend's status and that of its dependencies",
        "description" : "Returns 200 when every dependency is reachable and 503 when any is not, so an uptime check can rely on the status code alone rather than parsing the body.\n",
        "operationId" : "health",
        "responses" : {
          "200" : {
            "description" : "Every dependency is reachable",
            "content" : {
              "application/json" : {
                "schema" : {
                  "$ref" : "#/components/schemas/HealthStatus"
                }
              }
            }
          },
          "503" : {
            "description" : "At least one dependency is unreachable",
            "content" : {
              "application/json" : {
                "schema" : {
                  "$ref" : "#/components/schemas/HealthStatus"
                }
              }
            }
          }
        }
      }
    }
  },
  "components" : {
    "schemas" : {
      "UserCreateRequest" : {
        "type" : "object",
        "description" : "What creating an Unclaimed User asks the widget to prove, if this deployment has one",
        "properties" : {
          "turnstileToken" : {
            "type" : "string",
            "description" : "The Cloudflare Turnstile widget's token, absent where no widget is configured"
          }
        }
      },
      "User" : {
        "type" : "object",
        "description" : "Anyone who has played, whether or not they have Claimed",
        "properties" : {
          "id" : {
            "type" : "string",
            "format" : "uuid",
            "description" : "Stable identifier",
            "example" : "3f1c0b1a-5c2e-4d3b-9f8a-1e2d3c4b5a69"
          },
          "handle" : {
            "type" : "string",
            "description" : "The User's Handle",
            "example" : "PERCOLATING_FERRET"
          },
          "claimed" : {
            "type" : "boolean",
            "description" : "Whether credentials have been attached to this User",
            "example" : false
          }
        },
        "required" : [ "claimed", "handle", "id" ]
      },
      "ClaimRequest" : {
        "type" : "object",
        "description" : "What the player chooses on Claiming",
        "properties" : {
          "handle" : {
            "type" : "string",
            "description" : "The Handle to Claim with, retiring the generated one. Ignored when signing in merges into an account that already has one",
            "example" : "PERCOLATING_FERRET",
            "maxLength" : 22,
            "minLength" : 3,
            "pattern" : "^[A-Za-z0-9_]+$"
          }
        },
        "required" : [ "handle" ]
      },
      "TypingRunSubmission" : {
        "type" : "object",
        "description" : "The raw data a finished Run is verified from",
        "properties" : {
          "issueId" : {
            "type" : "string",
            "format" : "uuid",
            "description" : "The Issue this Run answers"
          },
          "typedText" : {
            "type" : "string",
            "description" : "The text as typed, in full",
            "minLength" : 1
          },
          "keystrokes" : {
            "type" : "integer",
            "format" : "int32",
            "description" : "Total character keystrokes, mistakes included",
            "example" : 312
          },
          "startedAt" : {
            "type" : "string",
            "format" : "date-time",
            "description" : "The client's clock at the first keystroke"
          },
          "completedAt" : {
            "type" : "string",
            "format" : "date-time",
            "description" : "The client's clock at the last keystroke"
          }
        },
        "required" : [ "completedAt", "issueId", "keystrokes", "startedAt", "typedText" ]
      },
      "TypingRun" : {
        "type" : "object",
        "description" : "A completed and verified Run against a Passage",
        "properties" : {
          "id" : {
            "type" : "string",
            "format" : "uuid"
          },
          "passageId" : {
            "type" : "string",
            "format" : "uuid"
          },
          "discipline" : {
            "type" : "string",
            "description" : "One of the three ways to play",
            "enum" : [ "QUOTES", "PROSE", "CODE" ]
          },
          "wpm" : {
            "type" : "number",
            "description" : "Correct characters over five, per minute, computed by the server",
            "example" : 78.4
          },
          "accuracy" : {
            "type" : "number",
            "description" : "Correct keystrokes over total keystrokes, as a percentage",
            "example" : 99.1
          },
          "elapsedMillis" : {
            "type" : "integer",
            "format" : "int32",
            "description" : "How long the Run took",
            "example" : 31240
          },
          "keystrokes" : {
            "type" : "integer",
            "format" : "int32",
            "description" : "Total character keystrokes, mistakes included",
            "example" : 312
          },
          "correctCharacters" : {
            "type" : "integer",
            "format" : "int32",
            "description" : "Characters of the Passage the final text got right",
            "example" : 309
          },
          "errors" : {
            "type" : "integer",
            "format" : "int32",
            "description" : "Keystrokes that were not correct characters",
            "example" : 3
          },
          "completedAt" : {
            "type" : "string",
            "format" : "date-time"
          },
          "personalBest" : {
            "type" : "boolean",
            "description" : "Whether this Run is a new Personal Best in its Discipline",
            "example" : true
          },
          "previousBestWpm" : {
            "type" : "number",
            "description" : "The Personal Best this Run beat, when it beat one",
            "example" : 112.0
          }
        },
        "required" : [ "accuracy", "completedAt", "correctCharacters", "discipline", "elapsedMillis", "errors", "id", "keystrokes", "passageId", "personalBest", "wpm" ]
      },
      "Rejection" : {
        "type" : "object",
        "description" : "A submitted Run that was not recorded, and why",
        "properties" : {
          "reason" : {
            "type" : "string",
            "description" : "Why a submitted Run was not recorded",
            "enum" : [ "NO_SUCH_ISSUE", "ISSUE_EXPIRED", "ISSUE_ALREADY_USED", "ISSUE_SUPERSEDED", "PASSAGE_MISMATCH", "IMPOSSIBLE_DURATION", "IMPLAUSIBLE_KEYSTROKES", "IMPLAUSIBLE_SPEED" ]
          },
          "explanation" : {
            "type" : "string",
            "description" : "Human-readable restatement of the reason",
            "example" : "The typed text does not correspond to the Passage that was issued."
          }
        },
        "required" : [ "explanation", "reason" ]
      },
      "SolveRunSubmission" : {
        "type" : "object",
        "description" : "The raw data a finished Solve Run is judged and verified from",
        "properties" : {
          "issueId" : {
            "type" : "string",
            "format" : "uuid",
            "description" : "The Issue this Solve Run answers"
          },
          "source" : {
            "type" : "string",
            "description" : "The lines written below the Scaffold, exactly as typed",
            "example" : "    return []",
            "maxLength" : 16384,
            "minLength" : 0
          },
          "keystrokes" : {
            "type" : "integer",
            "format" : "int32",
            "description" : "Every character key pressed, including ones later deleted",
            "example" : 180
          },
          "startedAt" : {
            "type" : "string",
            "format" : "date-time",
            "description" : "The client's clock at the first keystroke"
          },
          "completedAt" : {
            "type" : "string",
            "format" : "date-time",
            "description" : "The client's clock when the Solve Run was submitted"
          }
        },
        "required" : [ "completedAt", "issueId", "keystrokes", "source", "startedAt" ]
      },
      "SolveRun" : {
        "type" : "object",
        "description" : "A completed and judged Run against a Pattern",
        "properties" : {
          "id" : {
            "type" : "string",
            "format" : "uuid"
          },
          "patternId" : {
            "type" : "string",
            "format" : "uuid"
          },
          "patternSlug" : {
            "type" : "string",
            "example" : "hash-map-seen-lookup"
          },
          "verdict" : {
            "type" : "string",
            "enum" : [ "passed", "failed", "timeout", "error" ]
          },
          "testsPassed" : {
            "type" : "integer",
            "format" : "int32",
            "example" : 6
          },
          "testsTotal" : {
            "type" : "integer",
            "format" : "int32",
            "example" : 6
          },
          "elapsedMillis" : {
            "type" : "integer",
            "format" : "int32",
            "description" : "How long the Solve Run took, as verified",
            "example" : 48210
          },
          "keystrokes" : {
            "type" : "integer",
            "format" : "int32",
            "description" : "Total character keystrokes",
            "example" : 180
          },
          "wpm" : {
            "type" : "number",
            "description" : "Every submitted character over five, per minute — a secondary reading",
            "example" : 41.2
          },
          "completedAt" : {
            "type" : "string",
            "format" : "date-time"
          },
          "personalBest" : {
            "type" : "boolean",
            "description" : "Whether this Run is a new Personal Best in the Code Discipline",
            "example" : true
          },
          "previousBestWpm" : {
            "type" : "number",
            "description" : "The Personal Best this Run beat, when it beat one",
            "example" : 38.4
          }
        },
        "required" : [ "completedAt", "elapsedMillis", "id", "keystrokes", "patternId", "patternSlug", "personalBest", "testsPassed", "testsTotal", "verdict", "wpm" ]
      },
      "Unjudged" : {
        "type" : "object",
        "description" : "A Solve Run that could not be judged. No Run was recorded and the Challenge is still live",
        "properties" : {
          "explanation" : {
            "type" : "string",
            "example" : "The judge could not be reached. Your Challenge is still yours."
          }
        },
        "required" : [ "explanation" ]
      },
      "ExampleTest" : {
        "type" : "object",
        "description" : "A test case shown to the player before they start",
        "properties" : {
          "name" : {
            "type" : "string",
            "description" : "What the case is about",
            "example" : "the pair is the first two numbers"
          },
          "call" : {
            "type" : "string",
            "description" : "The call the submitted source must satisfy",
            "example" : "pair_sum([2, 7, 11, 15], 9)"
          },
          "expected" : {
            "type" : "string",
            "description" : "The answer the call has to produce",
            "example" : [ 0, 1 ]
          }
        },
        "required" : [ "call", "expected", "name" ]
      },
      "Pattern" : {
        "type" : "object",
        "description" : "A Pattern to solve, as the player is shown it",
        "properties" : {
          "id" : {
            "type" : "string",
            "format" : "uuid"
          },
          "slug" : {
            "type" : "string",
            "description" : "Stable identifier, also what the judge knows this Pattern by",
            "example" : "hash-map-seen-lookup"
          },
          "name" : {
            "type" : "string",
            "example" : "Store what you've seen, look up what you need"
          },
          "family" : {
            "type" : "string",
            "description" : "The grouping a Pattern belongs to",
            "enum" : [ "HASH_MAP", "TWO_POINTERS", "SLIDING_WINDOW", "STACK", "HEAP", "BINARY_SEARCH", "GRAPH", "DYNAMIC_PROGRAMMING" ]
          },
          "seniority" : {
            "type" : "string",
            "description" : "The difficulty band of a Challenge",
            "enum" : [ "JUNIOR", "SENIOR", "PRINCIPAL" ]
          },
          "prompt" : {
            "type" : "string",
            "description" : "The technique and what to do with it, in the player's words"
          },
          "scaffold" : {
            "type" : "string",
            "description" : "The read-only lines above the editable region",
            "example" : "def pair_sum(numbers, target):"
          },
          "exampleTests" : {
            "type" : "array",
            "items" : {
              "$ref" : "#/components/schemas/ExampleTest"
            }
          }
        },
        "required" : [ "exampleTests", "family", "id", "name", "prompt", "scaffold", "seniority", "slug" ]
      },
      "SolveChallenge" : {
        "type" : "object",
        "description" : "A Pattern to solve, and the Issue that recorded it going out",
        "properties" : {
          "issueId" : {
            "type" : "string",
            "format" : "uuid",
            "description" : "What the client hands back when the Solve Run is submitted"
          },
          "pattern" : {
            "$ref" : "#/components/schemas/Pattern"
          },
          "expiresAt" : {
            "type" : "string",
            "format" : "date-time",
            "description" : "After this moment the Challenge can no longer be answered"
          }
        },
        "required" : [ "expiresAt", "issueId", "pattern" ]
      },
      "PatternActivation" : {
        "type" : "object",
        "description" : "What the activation gate made of one Pattern",
        "properties" : {
          "slug" : {
            "type" : "string",
            "example" : "hash-map-seen-lookup"
          },
          "activated" : {
            "type" : "boolean"
          },
          "explanation" : {
            "type" : "string",
            "description" : "What happened, in a sentence",
            "example" : "The reference solution passed all 6 tests."
          }
        },
        "required" : [ "activated", "explanation", "slug" ]
      },
      "ChallengeRequest" : {
        "type" : "object",
        "description" : "A request for a Challenge in one Discipline",
        "properties" : {
          "discipline" : {
            "type" : "string",
            "description" : "One of the three ways to play",
            "enum" : [ "QUOTES", "PROSE", "CODE" ]
          }
        },
        "required" : [ "discipline" ]
      },
      "Challenge" : {
        "type" : "object",
        "description" : "A Passage to transcribe, and the Issue that recorded it going out",
        "properties" : {
          "issueId" : {
            "type" : "string",
            "format" : "uuid"
          },
          "passage" : {
            "$ref" : "#/components/schemas/Passage"
          },
          "expiresAt" : {
            "type" : "string",
            "format" : "date-time",
            "description" : "After this moment the Challenge can no longer be answered"
          }
        },
        "required" : [ "expiresAt", "issueId", "passage" ]
      },
      "Passage" : {
        "type" : "object",
        "description" : "A fixed piece of text to be transcribed",
        "properties" : {
          "id" : {
            "type" : "string",
            "format" : "uuid"
          },
          "discipline" : {
            "type" : "string",
            "description" : "One of the three ways to play",
            "enum" : [ "QUOTES", "PROSE", "CODE" ]
          },
          "text" : {
            "type" : "string",
            "description" : "The text to transcribe, printable ASCII throughout",
            "example" : "Call me Ishmael."
          },
          "attribution" : {
            "type" : "string",
            "description" : "Who said or wrote it",
            "example" : "Herman Melville, Moby-Dick, 1851"
          },
          "characterCount" : {
            "type" : "integer",
            "format" : "int32",
            "description" : "How many characters the text is",
            "example" : 302
          }
        },
        "required" : [ "attribution", "characterCount", "discipline", "id", "text" ]
      },
      "HistoryEntry" : {
        "type" : "object",
        "description" : "One Run in a User's history, of either kind",
        "properties" : {
          "runId" : {
            "type" : "string",
            "format" : "uuid"
          },
          "discipline" : {
            "type" : "string",
            "description" : "One of the three ways to play",
            "enum" : [ "QUOTES", "PROSE", "CODE" ]
          },
          "wpm" : {
            "type" : "number",
            "example" : 118.4
          },
          "verdict" : {
            "type" : "string",
            "description" : "The Verdict, for a Solve Run. Absent for a Typing Run.",
            "enum" : [ "passed", "failed", "timeout", "error" ]
          },
          "completedAt" : {
            "type" : "string",
            "format" : "date-time"
          }
        },
        "required" : [ "completedAt", "discipline", "runId", "wpm" ]
      },
      "PersonalBest" : {
        "type" : "object",
        "description" : "A User's highest WPM within one Discipline, derived from their Runs",
        "properties" : {
          "discipline" : {
            "type" : "string",
            "description" : "One of the three ways to play",
            "enum" : [ "QUOTES", "PROSE", "CODE" ]
          },
          "wpm" : {
            "type" : "number",
            "example" : 148.0
          }
        },
        "required" : [ "discipline", "wpm" ]
      },
      "Profile" : {
        "type" : "object",
        "description" : "A User's Personal Bests and recent Runs, derived at query time",
        "properties" : {
          "user" : {
            "$ref" : "#/components/schemas/User"
          },
          "personalBests" : {
            "type" : "array",
            "items" : {
              "$ref" : "#/components/schemas/PersonalBest"
            }
          },
          "bestAccuracy" : {
            "type" : "number",
            "description" : "Highest Accuracy across Typing Runs",
            "example" : 99.2
          },
          "recentAverageWpm" : {
            "type" : "number",
            "description" : "Mean WPM across the Runs in history",
            "example" : 126.4
          },
          "history" : {
            "type" : "array",
            "items" : {
              "$ref" : "#/components/schemas/HistoryEntry"
            }
          }
        },
        "required" : [ "history", "personalBests", "user" ]
      },
      "Leaderboard" : {
        "type" : "object",
        "description" : "A ranking of Users by their best Run on one Challenge, derived at query time",
        "properties" : {
          "passageId" : {
            "type" : "string",
            "format" : "uuid"
          },
          "discipline" : {
            "type" : "string",
            "description" : "One of the three ways to play",
            "enum" : [ "QUOTES", "PROSE", "CODE" ]
          },
          "entries" : {
            "type" : "array",
            "items" : {
              "$ref" : "#/components/schemas/LeaderboardEntry"
            }
          },
          "you" : {
            "$ref" : "#/components/schemas/LeaderboardEntry",
            "description" : "The requesting User's own row, wherever they stand"
          },
          "participants" : {
            "type" : "integer",
            "format" : "int32",
            "description" : "Distinct Users with a Run on this Challenge",
            "example" : 37
          },
          "minimumParticipants" : {
            "type" : "integer",
            "format" : "int32",
            "description" : "Distinct Users needed before the ranking is shown",
            "example" : 5
          }
        },
        "required" : [ "discipline", "entries", "minimumParticipants", "participants", "passageId" ]
      },
      "LeaderboardEntry" : {
        "type" : "object",
        "description" : "One User's best Run on one Challenge, and where it ranks",
        "properties" : {
          "position" : {
            "type" : "integer",
            "format" : "int32",
            "description" : "Standing, with ties sharing a position",
            "example" : 2
          },
          "user" : {
            "$ref" : "#/components/schemas/User"
          },
          "wpm" : {
            "type" : "number",
            "description" : "WPM of this User's best Run on this Challenge",
            "example" : 141.0
          },
          "accuracy" : {
            "type" : "number",
            "description" : "Accuracy of that same Run",
            "example" : 98.7
          }
        },
        "required" : [ "accuracy", "position", "user", "wpm" ]
      },
      "HealthStatus" : {
        "type" : "object",
        "description" : "The backend's own status and that of its dependencies",
        "properties" : {
          "status" : {
            "type" : "string",
            "description" : "Overall status",
            "enum" : [ "UP", "DEGRADED" ],
            "example" : "UP"
          },
          "database" : {
            "type" : "string",
            "description" : "PostgreSQL connectivity",
            "enum" : [ "UP", "DEGRADED" ],
            "example" : "UP"
          },
          "judge" : {
            "type" : "string",
            "description" : "Judge availability. DEGRADED means the Code Discipline is unavailable; the rest of the site is unaffected.",
            "enum" : [ "UP", "DEGRADED" ],
            "example" : "UP"
          },
          "version" : {
            "type" : "string",
            "description" : "Running build version",
            "example" : "0.0.1-SNAPSHOT"
          }
        },
        "required" : [ "database", "judge", "status", "version" ]
      }
    },
    "securitySchemes" : {
      "bearerAuth" : {
        "type" : "http",
        "description" : "The identity provider's ID token, validated by the backend as a resource server (ADR-0011).",
        "scheme" : "bearer",
        "bearerFormat" : "JWT"
      }
    }
  }
}
